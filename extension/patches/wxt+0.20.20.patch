diff --git a/node_modules/wxt/dist/core/builders/vite/plugins/download.mjs b/node_modules/wxt/dist/core/builders/vite/plugins/download.mjs
index d96057e..c6310f1 100644
--- a/node_modules/wxt/dist/core/builders/vite/plugins/download.mjs
+++ b/node_modules/wxt/dist/core/builders/vite/plugins/download.mjs
@@ -1,3 +1,5 @@
+import { readFile } from "node:fs/promises";
+import path from "node:path";
 import { fetchCached } from "../../../utils/network.mjs";
 //#region src/core/builders/vite/plugins/download.ts
 /**
@@ -20,7 +22,12 @@ function download(config) {
 		load: {
 			filter: { id: /^\x00url:/ },
 			handler(id) {
-				return fetchCached(id.replace("\0url:", ""), config);
+				let url = id.replace("\0url:", "");
+				while (url.startsWith("url:")) url = url.slice(4);
+				const isWinAbs = /^[A-Za-z]:[\\/]/.test(url);
+				const isPosixAbs = url.startsWith("/") && !url.startsWith("//");
+				if (isWinAbs || isPosixAbs) return readFile(path.normalize(url), "utf-8");
+				return fetchCached(url, config);
 			}
 		}
 	};
